Good commit messages

- Use a short, imperative summary: `type(scope): short description`
- Explain the why in the body when the change is non-obvious
- Keep related changes in a single commit
- Examples:
  - `feat(chat): add thread unread counts`
  - `fix(server): handle DB reconnect errors`

This repo uses concise, conventional-style commit messages for clarity.
